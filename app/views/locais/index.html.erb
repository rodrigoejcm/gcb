
<h1>Meus Cadastrados de Locais</h1>

<div class="alert alert-info" role="alert">
    Estes são os locais cadastrados por você. Estes locais estão disponiveis para serem selecionados na tela de cadastro de turmas..
  </div>

<p id="notice"><%= notice %></p>

<table class="table table-hover table-striped">
  <thead>
    <tr>
      <th>Local</th>
      <th></th>
      <th class="text-center">Contatos</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @locais.each do |local| %>
      <tr>
        <td><%=image_tag local.imagem_url.to_s, size: '70'%></td>
        <td>
           <%=local.nome%><br>
           <%=local.endereco%><br>
           <%=local.cidade%><br>
           <%=local.estadoprovincia%> - <%=local.pais%>
        </td>
        <td class="text-center">
           <%=local.contato1%><br>
           <%=local.contato2%><br>
        </td>
        <td>
          
          <%= link_to(local) do %>
            <i class="fa fa-calendar-o"></i> Visualizar
          <% end %>
          <br>
          <%= link_to(edit_local_path(local)) do %>
            <i class="fa fa-pencil"></i> Editar
          <% end %>
          <br>
          <%= link_to(local, method: :delete, data: { confirm: 'Tem certeza que deseja excluir este local?' }) do %>
            <i class="fa fa-trash-o"></i> Excluir
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= will_paginate @locais %>

<br>


<%= link_to turmas_path do %>
  <i class="fa fa-users"></i> Voltar para tela de Turmas
<% end %>
